# Auth0 Configuration
# Copy this file to .env.local and fill in your Auth0 credentials

# Generate a random string for the secret (at least 32 characters)
# You can use: openssl rand -hex 32
AUTH0_SECRET=

# Your app's base URL (use http://localhost:3000 for development)
AUTH0_BASE_URL=http://localhost:3000

# Your Auth0 tenant domain (e.g., https://your-tenant.auth0.com)
AUTH0_ISSUER_BASE_URL=

# Your Auth0 application's Client ID
AUTH0_CLIENT_ID=

# Your Auth0 application's Client Secret
AUTH0_CLIENT_SECRET=

# ============================================
# Auth0 Setup Instructions:
# ============================================
# 1. Go to https://auth0.com and create a free account
# 2. Create a new "Regular Web Application"
# 3. Go to Settings tab and find:
#    - Domain → use as AUTH0_ISSUER_BASE_URL (add https:// prefix)
#    - Client ID → use as AUTH0_CLIENT_ID
#    - Client Secret → use as AUTH0_CLIENT_SECRET
# 4. In Settings, add to "Allowed Callback URLs":
#    http://localhost:3000/auth/callback
# 5. In Settings, add to "Allowed Logout URLs":
#    http://localhost:3000
# 6. Save changes
# ============================================
